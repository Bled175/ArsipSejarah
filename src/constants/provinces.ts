export interface ProvinceData {
  id: string;
  name: string;
  path: string; // SVG path
}

export const INDONESIA_PROVINCES: ProvinceData[] = [
  // This is a simplified set of paths for demonstration if I can find them.
  // Since I might not be able to get all 38 paths easily in one go, 
  // I will focus on the major islands first or use a library.
  // Actually, I'll try to find a reasonably comprehensive list.
];
